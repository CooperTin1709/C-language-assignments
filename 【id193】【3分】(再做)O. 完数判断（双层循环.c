/*题目描述

一个数如果恰好等于它的因子之和，这个数就称为“完数”。例如6的因子为1，2，3，
而6=1+2+3；因此是“完数”。编程对输入的数字(假设该数字小于1000)判断是否是“完数”并输出其所有因子。

输入

判断的次数和每次输入的数字

输出

该数是否完数和其所有因子*/
#include <stdio.h>
int n;
int a[1000];

int yinzi(int k)
{
    int sum = 0;
    for (int i = 1; i <= k / 2; i++)
    {

    }
}

int main(void)
{
    int t = 0;
    scanf("%d", &t);
    while (t--)
    {
        int a[]={0};
        scanf("%d", &n);
    }
}
